# 鲜直达 MVP · 访问链接与账号手册

> 文档最后更新：2026-09-04
> 项目路径：`/workspace/xzd-mvp/`

## 一、运行方式

### 1. 启动后端服务（所有端共用）

```bash
cd /workspace/xzd-mvp/server
npm install            # 首次或依赖丢失时执行
node src/app.js
```

启动成功后会看到：

```
🚀 鲜直达 全栈服务已启动
   PC 后台:  http://localhost:4000/admin/
   移动端:    http://localhost:4000/mp/
   API:       http://localhost:4000/api/health
```

服务监听 `0.0.0.0:4000`，统一对外提供 API + PC 后台 + 移动端三个入口。

### 2. 访问入口

所有入口都从沙箱内的预览面板打开（不是用户本地 PC 的 localhost）。

| 端           | 访问地址                                              | 用途                        |
| ----------- | ------------------------------------------------- | ------------------------- |
| PC 管理后台     | `http://localhost:4000/admin/`                    | 运营人员管理商品、订单、用户、合伙人、佣金     |
| 移动端 H5      | `http://localhost:4000/mp/`                       | 用户端：小程序模拟器，下单 / 查订单 / 看佣金 |
| 移动端根路径      | `http://localhost:4000/`                          | 等同于 `/mp/`（默认首页）          |
| API 健康检查    | `http://localhost:4000/api/health`                | 查看服务在线状态与数据量              |
| API 文档（自描述） | `http://localhost:4000/api/orders/_meta/statuses` | 订单状态字典                    |

> 注意：所有端都用 `localhost:4000`，不分开端口；如果看到其他端口（如 5173 / 8080），那是旧的。

### 3. 停止服务

后台运行（`&`）时：

```bash
ps aux | grep "node src/app.js" | grep -v grep | awk '{print $2}' | xargs -r kill
```

或通过 `StopCommand` 工具终止沙箱内的命令。

## 二、登录账号

### PC 管理后台

- 地址：`http://localhost:4000/admin/`

- 账号：`admin`

- 密码：`admin123`

- 角色：超级管理员（全部权限）

### 移动端 H5

普通用户和合伙人共用移动端登录入口，根据账号自动判断身份。

| 角色        | 账号            | 密码       | 备注           |
| --------- | ------------- | -------- | ------------ |
| ⭐ 合伙人（李姐） | `136****9012` | `123456` | 顶级合伙人，可作为邀请人 |
| ⭐ 合伙人（王芳） | `139****5678` | `123456` | 上级：李姐（U003）  |
| ⭐ 合伙人（老王） | `137****3456` | `123456` | 上级：李姐（U003）  |
| 普通用户（小张）  | `138****1234` | `123456` | 仅购买，无分销权限    |

> 所有测试账号的密码统一为 `123456`（管理后台管理员除外：admin / admin123）。
> 密码是种子数据写入时用 bcrypt 哈希的，不要在线上用这套弱口令。

## 三、数据快照（种子数据，启动时自动写入 SQLite）

| 表             | 条数 | 关键内容                                              |
| ------------- | -- | ------------------------------------------------- |
| `users`       | 5  | admin + 4 个测试用户（3 个合伙人 + 1 个普通）                   |
| `products`    | 6  | 草莓 / 脐橙 / 蜂蜜 / 大米 / 核桃 / 黄桃（P006 已下架）             |
| `orders`      | 14 | 覆盖 pending\_pay / paid / shipped / delivered 各种状态 |
| `commissions` | 4  | 待结算 / 已结算各 2 笔                                    |
| `logs`        | 5+ | 关键操作流水                                            |

> 数据库文件：`/workspace/xzd-mvp/server/data.db`
> 重置方法：删除 `data.db*` 三个文件后重启服务，会自动重新种子。

## 四、常见问题

### 1. 浏览器打开 `http://localhost:4000/admin/` 显示"无法访问"

- 服务可能在沙箱维护后被回收，**重新执行** `cd /workspace/xzd-mvp/server && node src/app.js`

- 一定要在沙箱**预览面板**里打开（不是用户本地 PC）

- 如果只打开 `localhost:4000` 根路径，会跳到移动端 H5，登录界面不一样

### 2. 登录报错「账号不存在」

- 账号是 `admin`（不是手机号格式），密码 `admin123`

- 用户账号要按上表逐字填写 `138****1234` 这种脱敏格式（带星号）

- 实际数据库里就是这个脱敏格式保存的

### 3. 登录报错「密码错误」

- admin 用 `admin123`，其他用户都是 `123456`

- 如果忘记密码，删除 `data.db*` 重启会自动恢复

### 4. 登录报错「网络连接失败」

通常是服务没启动，或端口被占用。检查：

```bash
curl http://localhost:4000/api/health
```

能返回 JSON 说明服务正常。

### 5. 移动端打开后界面是 PC 布局

- 移动端地址是 `/mp/`，不是 `/admin/`

- 推荐用浏览器开发者工具切到 iPhone 视口查看（375×812）

## 五、目录结构速查

```
/workspace/xzd-mvp/
├── public-admin/      ← 服务实际读取的 PC 后台（修改这里）
├── public-mp/         ← 服务实际读取的移动端（修改这里）
├── admin/             ← PC 后台源文件备份（与 public-admin 同步）
├── miniprogram/       ← 移动端源文件备份（与 public-mp 同步）
└── server/
    ├── src/
    │   ├── app.js           服务入口
    │   ├── db.js            SQLite 初始化 + 种子数据
    │   ├── auth.js          JWT 签发与校验
    │   └── routes/          auth / products / orders / admin
    ├── data.db              SQLite 数据库
    └── package.json
```

修改源文件时，改 `public-admin/` / `public-mp/`，**不需要重启服务**（静态文件即时生效）。
改 `server/src/` 下的代码需要重启 `node src/app.js`。

## 六、版本变更记录

| 日期         | 变更                                                         |
| ---------- | ---------------------------------------------------------- |
| 2026-09-04 | 统一服务到 4000 端口；API 增加用户/合伙人 CRUD；用户管理页面增加详情抽屉、订单/消费列、CSV 导出 |
| 2026-09-04 | 优化登录错误提示，区分网络失败 / 401 / 4xx                                |
| 2026-09-04 | 登录服务 / 路径 / 账号统一文档化（本文件）                                   |

> 后续如有变动，**直接修改本文件**即可。

